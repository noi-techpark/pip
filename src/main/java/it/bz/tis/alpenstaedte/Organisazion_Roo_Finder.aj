// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package it.bz.tis.alpenstaedte;

import it.bz.tis.alpenstaedte.Organisazion;
import javax.persistence.EntityManager;
import javax.persistence.TypedQuery;

privileged aspect Organisazion_Roo_Finder {
    
    public static Long Organisazion.countFindOrganisazionsByName(String name) {
        if (name == null || name.length() == 0) throw new IllegalArgumentException("The name argument is required");
        EntityManager em = Organisazion.entityManager();
        TypedQuery q = em.createQuery("SELECT COUNT(o) FROM Organisazion AS o WHERE o.name = :name", Long.class);
        q.setParameter("name", name);
        return ((Long) q.getSingleResult());
    }
    
    public static TypedQuery<Organisazion> Organisazion.findOrganisazionsByName(String name) {
        if (name == null || name.length() == 0) throw new IllegalArgumentException("The name argument is required");
        EntityManager em = Organisazion.entityManager();
        TypedQuery<Organisazion> q = em.createQuery("SELECT o FROM Organisazion AS o WHERE o.name = :name", Organisazion.class);
        q.setParameter("name", name);
        return q;
    }
    
    public static TypedQuery<Organisazion> Organisazion.findOrganisazionsByName(String name, String sortFieldName, String sortOrder) {
        if (name == null || name.length() == 0) throw new IllegalArgumentException("The name argument is required");
        EntityManager em = Organisazion.entityManager();
        StringBuilder queryBuilder = new StringBuilder("SELECT o FROM Organisazion AS o WHERE o.name = :name");
        if (fieldNames4OrderClauseFilter.contains(sortFieldName)) {
            queryBuilder.append(" ORDER BY ").append(sortFieldName);
            if ("ASC".equalsIgnoreCase(sortOrder) || "DESC".equalsIgnoreCase(sortOrder)) {
                queryBuilder.append(" ").append(sortOrder);
            }
        }
        TypedQuery<Organisazion> q = em.createQuery(queryBuilder.toString(), Organisazion.class);
        q.setParameter("name", name);
        return q;
    }
    
}
